NEXT_PUBLIC_APP_CODE=car-manager-v2
# Space-separated list of origins allowed to iframe-embed this app (CSP frame-ancestors).
# Include EVERY platform host that may proxy/embed:
#   - Local dev:    http://localhost:5200 https://*.amoeba.site
#   - Staging only: https://stg-apps.amoeba.site https://*.amoeba.site
#   - Prod only:    https://apps.amoeba.site https://ama.amoeba.site
NEXT_PUBLIC_AMA_ORIGIN=https://ama.amoeba.site
NEXT_PUBLIC_DEFAULT_LOCALE=vi

# Next.js basePath — set when served under ambAppStore at `/app-car-manager-v2/*`
# (nginx route or platform Vite proxy). Leave empty for standalone dev at :3001.
BASE_PATH=

# Canonical public origin override. REQUIRED in production behind reverse proxy
# when `x-forwarded-host` is not reliably forwarded. Used by lib/request-origin.ts
# for redirect URL construction (cookie set, session-expired redirect, ama_token strip).
# Examples:
#   - Render direct:  https://car-manager-staging.onrender.com
#   - Behind nginx:   https://apps.amoeba.site  (when basePath = /app-car-manager-v2)
# On Render, RENDER_EXTERNAL_URL is auto-set as fallback — leaving APP_URL empty also works.
APP_URL=

JWT_SECRET=replace-with-shared-secret-from-ambManagement
SESSION_COOKIE_NAME=amb_session

# Demo: enable /dev-login route to bypass AMA auth (role buttons on /session-expired)
# DO NOT enable on real production.
DEMO_AUTO_LOGIN=false

# Optional — base URL for `scripts/dev-token.mjs` (mints test JWT + prints paste-ready URL).
# Default http://localhost:3001. Override when testing against a deployed instance.
DEV_WEB_URL=

# Neon Postgres — free tier, region ap-southeast-1 (Singapore), pooler endpoint.
# Physical DB name = neondb (Neon default). See CLAUDE.md §4.3.
#   DATABASE_URL          = currently-active branch (used by `npm run db:migrate`, `db:studio`, web app)
#   DATABASE_URL_DEV      = dev branch (used by `npm run db:migrate:dev`)
#   DATABASE_URL_STAGING  = staging branch (used by `npm run db:migrate:staging`)
# Setup: copy DEV URL into both DATABASE_URL and DATABASE_URL_DEV (default local target).
DATABASE_URL=postgresql://neondb_owner:<password>@<dev-host>-pooler.<region>.aws.neon.tech/neondb?sslmode=require&channel_binding=require
DATABASE_URL_DEV=postgresql://neondb_owner:<password>@<dev-host>-pooler.<region>.aws.neon.tech/neondb?sslmode=require&channel_binding=require
DATABASE_URL_STAGING=postgresql://neondb_owner:<password>@<staging-host>-pooler.<region>.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# AWS S3 — chứng từ chi phí (NFR-11, retention 5 năm NFR-10)
AWS_REGION=ap-southeast-1
AWS_S3_BUCKET=amb-car-manager-v2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# AMA platform reference
APP_STORE_API_URL=https://apps.amoeba.site/api/v1
