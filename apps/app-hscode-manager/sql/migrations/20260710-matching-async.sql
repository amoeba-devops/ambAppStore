-- =====================================================================
-- HS Code 통관 에이전트 — 문서 업로드 매칭 대량 비동기(BullMQ)
-- 요청건에 처리 진행 카운터 추가(진행률 표시).
-- 대상: PostgreSQL 15. synchronize 비활성 → 수동 적용(스테이징 먼저). 기존 무변경.
-- =====================================================================

ALTER TABLE hsm_match_requests
  ADD COLUMN IF NOT EXISTS mrq_processed_count INTEGER NOT NULL DEFAULT 0;

-- =====================================================================
-- End — 적용: docker exec postgres-hscode psql -U hscode_app -d db_hsm -f (이 파일)
-- =====================================================================
