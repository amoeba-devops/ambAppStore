# 테스트완료보고서 — HS Code Manager 지식창고(Knowledge Base) 메뉴

- **문서 ID**: TR-20260710-지식창고메뉴
- **작성일**: 2026-07-10
- **대상 앱**: HS Code Manager (`/app-hscode`)
- **선행 문서**: [TC-20260710-지식창고메뉴](TC-20260710-지식창고메뉴.md), [PLAN-20260710-지식창고메뉴](../plan/PLAN-20260710-지식창고메뉴.md)
- **테스트 환경**: 로컬 개발(정적 검증). ⚠️ 라이브 스테이징 DB 미연결 → DB 의존 통합 TC는 **스테이징 배포(마이그레이션 적용) 후 수행 예정**.

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| 백엔드 빌드(`nest build`) | ✅ PASS |
| 프론트엔드 빌드(`tsc && vite build`) | ✅ PASS (1755 modules) |
| 타입체크(BE/FE `tsc --noEmit`) | ✅ PASS (에러 0) |
| i18n JSON 유효성(ko/en/vi/id) | ✅ PASS |
| 라우트/DI 배선(25 엔드포인트, 6 컨트롤러) | ✅ 정합(빌드 시 모듈 그래프 검증) |
| DB 의존 통합 TC(ITC-KB-*) | ⏳ 스테이징 배포 후 수행 |

---

## 2. 정적 검증 상세 (환경 무관 — 이번 수행)

| TC | 검증 방법 | 결과 |
|----|-----------|------|
| TC-KB-016/024/044/072, RC-KB-001~003 (권한 경계) | 전 쓰기 엔드포인트 `@SuperAdminOnly()`, 읽기 `@Auth()` 데코레이터 소스 확인 | ✅ 코드상 정합 |
| TC-KB-073 (도메인 격리) | KB 모듈이 `hsm_documents` Repository 직접 import 없음 — `KnowledgeIngestService` 주입 경유 확인(`kb-post.service.ts`) | ✅ PASS |
| TC-KB-041/042 (첨부 검증) | 50MB/화이트리스트 검증 로직 + `HSC-E8001/8002` 소스 확인(`kb-attachment.service.ts`) | ✅ 코드상 정합 |
| TC-KB-050~052 (번역 캐시) | 캐시 HIT 선반환 + `MANUAL` 미덮어쓰기 로직 확인(`kb-translation.service.ts`) | ✅ 코드상 정합 |
| TC-KB-080~082 (i18n) | id 로케일 등록 + 언어 셀렉터 4언어 + 하드코딩 없음(`t()` 사용) 확인 | ✅ PASS |
| NFR-KB-005 (컨벤션) | `hsm_kb_` prefix·`pk_/fk_/idx_/uq_`·Request snake_case/Response camelCase·Mapper static 확인 | ✅ PASS |
| RC-KB-006 (빌드/린트) | BE/FE 빌드 그린 | ✅ PASS |

---

## 3. 스테이징 수행 대기 TC (DB·런타임 의존)

아래는 스테이징에 **마이그레이션 적용 후** 수행한다.

- **선결**: `sql/migrations/20260710-knowledge-base.sql` 스테이징 DB 수동 적용 + 첨부 볼륨(`kb-attachments-data`) 마운트 확인.
- 대상: TC-KB-001~070(CRUD·목록·상세·조회수·대시보드·검색·첨부 업다운·번역·태그·RAG 승격), ITC-KB-001~005(E2E), RC-KB-004/005(기존 도메인·3언어 회귀).
- 특기: ITC-KB-004(RAG 승격 후 챗봇/매칭 검색 노출), ITC-KB-005(첨부 볼륨 영속: 재기동 후 다운로드)는 실제 컨테이너 환경 필수.

---

## 4. 발견 이슈 / 제약

| # | 내용 | 처리 |
|---|------|------|
| 1 | 드래그 정렬을 외부 라이브러리(@dnd-kit) 대신 **네이티브 HTML5 DnD**로 구현 | 의존성·번들 증가 회피(계획서 대비 개선). 기능 동일 |
| 2 | i18n `id`는 **KB 화면 키만** 번역, 그 외 화면은 ko 폴백 | 프로젝트-로컬 확장(REQ A5) 취지에 부합. 전면 id화는 후속 |
| 3 | 번역 API는 요청자 테넌트의 AI 키로 Claude 호출 | 키 미설정 시 `HSC-E6002` 반환(무중단 아님 — 명시적 에러) |
| 4 | 통합 TC 미수행(로컬 DB 미연결) | 스테이징 배포 후 수행 → 본 TR 갱신 |

---

## 5. 결론

정적/빌드 레벨 검증은 전부 통과했다. 기능·통합 검증은 스테이징 배포(마이그레이션 적용) 직후 §3 목록을 수행하고 본 보고서를 갱신한다.
