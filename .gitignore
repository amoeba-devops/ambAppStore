node_modules/
dist/
.turbo/
*.log
.env
.env.local
.env.staging
.env.production
# Timestamped .env snapshots taken before edits — contain live secrets.
.env.bak*
.DS_Store
coverage/
.nyc_output/
*.pem
.claude/
# Un-ignore v2 apps' .claude (skills + memory are committed project context)
!apps/app-sales-report-v2/.claude/
!apps/app-sales-report-v2/.claude/**
!apps/app-car-manager-v2/.claude/
!apps/app-car-manager-v2/.claude/**
*.tsbuildinfo

# Binary / temp files
*.xlsx
~\

# Untracked secret-bearing reports (full credentials — never commit)
docs/report/*.local.md

# 대화 로그 (세션 연속성용 로컬 기록 — git 동기화 제외)
docs/log/

# ambManagement
ambManagement/
