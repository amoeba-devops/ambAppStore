# TC-260814 — 승인된 org 사용자의 v2 앱 자동 진입 항구적 해결

- **작성일**: 2026-08-14
- **선행 문서**: [REQ-260814](../analysis/REQ-260814-승인된앱-자동진입-항구적해결.md), [PLN-260814](../plan/PLN-260814-승인된앱-자동진입-항구적해결.md)
- **테스트 대상**: `apps.amoeba.site` 플랫폼 카탈로그
- **주 검증 계정**: CargoRush(`CARGO434`) — `app-car-manager-v2` ACTIVE

## 1. 테스트 환경

| 항목 | 값 |
|---|---|
| 진입점 | `ama.amoeba.site` → 커스텀 앱 |
| 대상 앱 | `app-car-manager-v2`, `app-sales-report-v2` |
| 핵심 로그 지표 | `GET /api/v1/platform/subscriptions/entity/{entId}` 출현 여부 |
| 로그 위치 | `/var/log/nginx/apps.amoeba.site-access.log` |

## 2. 테스트 케이스

### 2.1 핵심 시나리오 (R1 — 자동 진입)

| ID | 시나리오 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| TC-01 | 승인된 org 자동 진입 | CARGO434 ACTIVE | AMA → 차량관리 v2 클릭 | 카탈로그에 머무르지 않고 **앱 대시보드로 자동 진입**. `/login` 미노출 |
| TC-02 | 구독 조회 실제 발생 | TC-01 직후 | access log 확인 | `subscriptions/entity/{entId}` 요청 **1건 이상**. (수정 전: 0건) |
| TC-03 | 앱 토큰 전달 | TC-01 직후 | access log 확인 | `GET /app-car-manager-v2?ama_token=…` 에 토큰 존재 |
| TC-04 | 세션 쿠키 발급 | TC-01 직후 | 브라우저 쿠키 확인 | `amb_session` HttpOnly 쿠키 존재 |
| TC-05 | sales-report-v2 동일 동작 | UIT327 ACTIVE | AMA → 매출리포트 v2 | TC-01과 동일 |

### 2.2 재발 방지 시나리오 (R2 — 핵심)

| ID | 시나리오 | 절차 | 기대 결과 |
|---|---|---|---|
| TC-06 | **F5 새로고침 (S1)** | 카탈로그 도달 후 F5 | entity 컨텍스트 유지. "신청하기" **미노출**. 수정 전 재발 지점 |
| TC-07 | 북마크 진입 (S2) | 토큰 없이 `/apps/app-car-manager-v2` 직접 열기 (동일 탭 세션) | sessionStorage 복원 → 정상 상태 표시 |
| TC-08 | 새 탭 진입 (S2') | 새 탭에서 토큰 없이 직접 열기 | 복원 불가 → **unknown 배너** 노출. "신청하기" 아님 |
| TC-09 | 토큰 만료 (S3) | `exp` 경과 토큰으로 진입 | unknown 배너 노출. 승인 사용자에게 재신청 요구하지 않음 |

### 2.3 회귀 방지 시나리오 (무회귀 확인)

| ID | 시나리오 | 절차 | 기대 결과 |
|---|---|---|---|
| TC-10 | 진짜 미신청 | 미신청 org로 진입 | 기존대로 **"신청하기"** 버튼 노출. unknown 배너 아님 |
| TC-11 | PENDING 상태 | 심사중 구독 | "심사중" 비활성 버튼 (기존 동작) |
| TC-12 | 구 iframe 파라미터 | `?from=iframe&ent_id=…&role=MASTER` | 파라미터 경로 우선 적용, 기존 동작 유지 |
| TC-13 | iframe 파라미터 우선순위 | 복원값 존재 + 파라미터 진입 | **파라미터 값이 우선** 적용 |
| TC-14 | 관리자 로그인 경로 | admin 로그인 후 카탈로그 | 자동 진입 미발동, 기존 인증 경로 정상 |
| TC-15 | v1 앱 무영향 | `app-car-manager`(v1) 진입 | 기존 동작 유지 |

### 2.4 루프 방지 (C2 — 최고 위험)

| ID | 시나리오 | 절차 | 기대 결과 |
|---|---|---|---|
| TC-16 | **무한 루프 미발생** | 자동 진입 후 앱이 카탈로그로 되돌림 | 자동 진입 **재발동 안 함**. 브라우저 히스토리 폭주 없음 |
| TC-17 | 뒤로가기 | 자동 진입 후 브라우저 뒤로가기 | 카탈로그에 머무름. 즉시 재진입 안 함 |
| TC-18 | 새 세션 재발동 | 새 탭에서 AMA 재진입 | 자동 진입 **정상 재발동** (가드가 영구 차단 아님) |

### 2.5 UI / i18n (R4, R5)

| ID | 시나리오 | 절차 | 기대 결과 |
|---|---|---|---|
| TC-19 | ko 번역 | 언어 ko | unknown 배너·전환 문구 한국어. raw 키 미노출 |
| TC-20 | en 번역 | 언어 en | 영어 정상 |
| TC-21 | vi 번역 | 언어 vi | 베트남어 정상 |
| TC-22 | 헤더 뱃지 | 폴백 컨텍스트 상태 | 빈 뱃지 미렌더 (FIX-260813 §3.4 유지) |

### 2.6 보안 (C3, C4)

| ID | 시나리오 | 절차 | 기대 결과 |
|---|---|---|---|
| TC-23 | JWT 미저장 | DevTools → Application | sessionStorage에 **원본 JWT 없음**. 식별자만 |
| TC-24 | localStorage 미사용 | DevTools 확인 | entity 컨텍스트가 localStorage에 **없음** |
| TC-25 | 탭 종료 시 소멸 | 탭 닫고 재오픈 | 컨텍스트 소멸 — 공용 PC 잔존 없음 |

### 2.7 빌드 검증

| ID | 항목 | 기대 결과 |
|---|---|---|
| TC-26 | `tsc -b` | exit 0, 에러 없음 |
| TC-27 | `vite build` | 빌드 성공 |
| TC-28 | 번들 반영 | `grep -c getInitialAmaClaims dist/assets/*.js` ≥ 1 |
| TC-29 | 배포 후 컨테이너 | `web-platform` healthy, `bff-platform`·`mysql-apps` **무중단** |

## 3. 합격 기준

| 구분 | 기준 |
|---|---|
| 필수 (Blocker) | TC-01, TC-02, TC-06, TC-10, TC-16 **전부 통과** |
| 중요 | TC-03~05, TC-07~09, TC-12~15, TC-23~25 통과 |
| 일반 | 나머지 통과 또는 경미 이슈 기록 |

**TC-02(구독 조회 요청 출현)와 TC-16(루프 미발생)은 반드시 확인한다** — 전자는 이번 수정의 성공 지표, 후자는 최대 위험 요소다.

## 4. 배포 후 즉시 확인 명령

```bash
# 구독 조회 요청 출현 (성공 지표 — 수정 전 0건)
sudo grep -c "subscriptions/entity" /var/log/nginx/apps.amoeba.site-access.log

# 루프 감지: 동일 IP의 /apps/ 반복 요청 급증 여부
sudo grep "GET /apps/app-car-manager-v2" /var/log/nginx/apps.amoeba.site-access.log \
  | tail -30

# 컨테이너 상태 (mysql-apps 무중단 확인)
sudo docker ps --format '{{.Names}}\t{{.Status}}'
```
