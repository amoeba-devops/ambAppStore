# 테스트완료보고서 — HS Code Manager AMA 컨텍스트 디버그 판넬

- **문서 ID**: TR-20260709-HSCode-디버그판넬
- **작성일**: 2026-07-09
- **선행 문서**: [TC-20260709](TC-20260709-HSCode-디버그판넬.md)
- **환경**: 로컬 빌드 + 스테이징 배포 검증

---

## 1. 테스트 결과

| ID | 시나리오 | 방법 | 결과 |
|----|----------|------|------|
| TC-01 | 우측하단 고정·기본 접힘 | 코드 검토(`fixed bottom-0 right-4 z-50`, `expanded=false`) | ✅ PASS |
| TC-02 | 펼치기/접기 토글 | 코드 검토(`setExpanded(v=>!v)`) | ✅ PASS |
| TC-03 | 사용자 요약(store+payload) | 코드 검토(`useAuthStore().user` + `decodeAmaToken`) | ✅ PASS (런타임 값 대조는 화면 확인) |
| TC-04 | 역할·admin 표기 | 코드 검토(`roles.join` + `isAdmin()`) | ✅ PASS |
| TC-05 | 만료 표기 | 코드 검토(`exp*1000<Date.now()` → valid/expired) | ✅ PASS |
| TC-06 | 원시 섹션(referrer/params/JWT/payload) | 코드 검토(textarea readOnly) | ✅ PASS |
| TC-07 | 복사(clipboard, 2초 복원) | 코드 검토(try/catch + setTimeout) | ✅ PASS |
| TC-08 | 무토큰 안전 | 코드 검토(옵셔널 체이닝, `'—'`/noToken 폴백) | ✅ PASS |
| TC-09 | i18n 3언어 | ko/en/vi `debug.*` 키 추가, JSON valid | ✅ PASS |
| TC-10 | 레이아웃 비간섭 | `fixed` 오버레이, App 레이아웃 끝 마운트 | ✅ PASS |
| TC-11 | 빌드 그린 | `npm run build` | ✅ PASS (1736 modules, 0 error) |

## 2. 배포 검증 (스테이징)
- rsync(5파일) → `web-app-hscode` 이미지 재빌드 → `up -d --no-deps`.
- 컨테이너 healthy, `https://stg-apps.amoeba.site/app-hscode/` **HTTP 200**.
- 서빙 번들(`assets/index-*.js`)에 디버그 판넬 i18n 문자열 **"AMA User Context" 존재 확인**.

## 3. 잔여(육안 확인 권장)
- TC-03/07의 실제 화면 렌더·클립보드 동작은 브라우저 육안 확인 항목(코드 로직은 검증됨).

## 4. 판정
- 필수 항목 전부 PASS. 근본 요구(우측하단 판넬 + AMA 사용자 정보 표현) 충족, 스테이징 라이브.
