# 풀 운전자 관리 — 작업계획서

**문서 ID**: PLAN-20260418-풀운전자관리
**작성일**: 2026-04-18
**참조**: REQ-20260418-풀운전자관리

---

## 1. 시스템 개발 현황 분석

### 기존 코드 현황

| 항목 | 상태 |
|------|------|
| BE `PATCH /drivers/:id/assign` | ✅ 구현 완료 |
| BE `PATCH /drivers/:id/unassign` | ✅ 구현 완료 |
| FE `driverApi.assign()` / `driverApi.unassign()` | ✅ 정의 완료 |
| FE `useDrivers()` 훅 | ✅ 목록 조회 완료 |
| FE assign/unassign mutation 훅 | ❌ 미구현 |
| FE PoolDriverModal | ❌ 미구현 |

### 제약사항

- Backend/DB 변경 없음
- POOL_DRIVER는 `cvhId = null` (미배정) 또는 특정 차량에 배정 가능
- 풀 운전자 조회: `GET /drivers` 전체 조회 후 role=POOL_DRIVER 필터링

---

## 2. 단계별 구현 계획

### Phase 1: useDrivers 훅에 assign/unassign mutation 추가

`hooks/useDrivers.ts`에 추가:
- `useAssignDriver()` — `PATCH /drivers/:id/assign`
- `useUnassignDriver()` — `PATCH /drivers/:id/unassign`
- 성공 시 driver lists invalidation

└─ 사이드 임팩트: 없음

### Phase 2: PoolDriverModal 컴포넌트 신규

- `components/driver/PoolDriverModal.tsx`
- Props: `{ open, onClose, vehicleId }`
- 법인 내 전체 POOL_DRIVER 조회 → 배정/미배정 분리
- "배정" 버튼 → `useAssignDriver()`
- "해제" 버튼 → `useUnassignDriver()`
- 배정/해제 후 목록 자동 갱신

└─ 사이드 임팩트: 없음 (신규 파일)

### Phase 3: VehicleDetailPage 버튼 연결

- "풀 운전자 관리" 버튼에 onClick → PoolDriverModal 열기
- `useState`로 모달 open 상태 관리

└─ 사이드 임팩트: 기존 UI 변경 없음, 모달만 추가

### Phase 4: i18n 번역 키 추가

`poolDriver` 섹션 추가 (ko/en/vi)

---

## 3. 변경 파일 목록

| 구분 | 파일 | 변경유형 |
|------|------|---------|
| FE | `hooks/useDrivers.ts` | 수정 |
| FE | `components/driver/PoolDriverModal.tsx` | **신규** |
| FE | `pages/VehicleDetailPage.tsx` | 수정 |
| i18n | `locales/{ko,en,vi}/car.json` | 수정 |

## 4. 사이드 임팩트

| 범위 | 위험도 | 설명 |
|------|--------|------|
| Backend | 없음 | 변경 없음 |
| 기존 운전자 탭 | 낮음 | 모달 추가만, 기존 목록 영향 없음 |
| DriverFormModal | 없음 | 독립적 |

## 5. DB 마이그레이션

**불필요**
